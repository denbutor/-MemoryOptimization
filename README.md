Output:
Execution time (milliseconds): 14
---
Пропозиції виправлення
---
1. Неефективність Пошуку унікальних слів: Використання distinctString.contains(words[i]) у циклі - проблема. Для ефективного пошуку краще використовувати HashSet.
2. Неефективність Підрахунку частоти. Використання вкладених циклів не ефективне, краще використовувати HashMap<String, Integer>.
3. Arrays.sort(words); на початку не дає переваги для подальшої логіки (оскільки масив відсортований) це є марною витратою часу.
4. Об'єднання слова та його частоти в один рядок неефективно (distincts[i] += " " + freq[i];), бо треба парсити цей рядок під час сортування (Integer.valueOf(str.replaceAll("[^0-9]", ""))),.
5. Метод replaceAll("[^0-9]", "") для витягнення числа зламається, якщо у тексті будуть слова, що містять схоже ( "chapter1" або "93").
6. Сортувати весь масив слів, щоб отримати лише 30 перших - не ефективно, краще використати PriorityQueue (Min-Heap) розміром 30, щоб знайти "Top K" елементів.
7. Метод cleanText ніколи не викликається, отже не потрібен.
8. Логіка очищення тексту (replaceAll...toLowerCase) дублюється (в методі cleanText і в main).
9. Шлях до файлу ("src/edu/pro/txt/harry.txt") винести у константу.
10. Кількість слів для виводу (30) є "магічним числом", тому треба винести у константу.